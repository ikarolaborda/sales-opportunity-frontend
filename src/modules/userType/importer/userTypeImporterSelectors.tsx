import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const userTypeImporterSelectors = importerSelectors(
  'userType.importer',
);

export default userTypeImporterSelectors;
